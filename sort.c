#include<stdio.h>

void main()
    {
  int arr[15],i,j,temp;
  printf("enter 15 elements of unsorted array");
  //input all 15 element
  
  for(i=0;i<=14;i++)
    scanf("%d,&arr[i]");              //curve blacket are not neccessary because its have single line statement.
  
  //sort the element of array
  printf("sorted array is:\n");
  
  for(j=i+1;j<=14;j++)
    {
    if(a[i]>a[j]){
      
    }
  }
  
  
    }